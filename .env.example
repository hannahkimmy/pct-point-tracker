# Environment Variables
# Copy this to .env and fill in your values

# Server Port (set automatically by hosting platform)
PORT=3000

# JWT Secret - Generate a strong random string for production
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-this-secret-in-production

# Database Path (for SQLite - only used if DATABASE_URL is not set)
# DATABASE_PATH=./pcpoints.sqlite

# PostgreSQL Database URL (for production - set by hosting platform)
# Format: postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://...
